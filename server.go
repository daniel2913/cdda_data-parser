package main

var base = "/users/user/documents/cdda/data/json/items"
var guns = "gun"
var ammo = "ammo"
var magazines = "magazine"

func main() {
	//getGuns()
	getAmmo()
	/* store["John"] = "John"
	http.HandleFunc("/", hello)
	http.HandleFunc("/auth", auth)
	http.HandleFunc("/register", register)

	err := http.ListenAndServe(":3000", nil)
	if err != nil {
		if errors.Is(err, http.ErrServerClosed) {
			fmt.Printf("Server closed\n")
		} else {
			fmt.Printf("Unknowm error: %s\n", err)
		}
	} */
}

//y0_AgAAAAAFKfILAAG8XgAAAAD-BXVxAADl5Y8RQHhGYIL0hozCXIR78r3g6A
